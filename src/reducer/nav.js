// import AppNavigation from '../navigation/stack/AppNavigator'

// export default (state, action ) => {
//     let { type, payload } = action
//     return AppNavigation.router.getStateForAction(action,state) || state
// }