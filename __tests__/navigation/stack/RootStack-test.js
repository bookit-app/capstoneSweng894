import RootStack from '../../../src/navigation/stack/RootStack'

describe('RootStack testing', () => {
    test('RootStack testing', () => {
        expect(RootStack.router.Setting).toEqual(undefined)
    })
})