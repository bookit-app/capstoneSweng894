import SettingPrefNavigator from '../../../../src/navigation/stack/account/SettingPrefNavigator'

describe('Setting Pref Navigator testing', () => {
    test('Setting Pref Navigator testing', () => {
        expect(SettingPrefNavigator.router.Setting).toEqual(undefined)
    })
})