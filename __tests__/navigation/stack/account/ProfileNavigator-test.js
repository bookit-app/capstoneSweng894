import ProfileNavigator from '../../../../src/navigation/stack/account/ProfileNavigator'

describe('Profile Navigator testing', () => {
    test('Profile Navigator testing', () => {
        expect(ProfileNavigator.router.Setting).toEqual(undefined)
    })
})